{
    // See https://go.microsoft.com/fwlink/?LinkId=733558 
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Before testing Google-Cloud-Functions/budget-control",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}/Google-Cloud-Functions/budget-control"
            },
            "command": "npm run pretest"
        },
        {
            "label": "Before testing Google-Cloud-Functions/compute-instance",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}/Google-Cloud-Functions/compute-instance"
            },
            "command": "npm run pretest"
        }
    ]
}
